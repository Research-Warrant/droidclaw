DATABASE_URL="postgres://user:password@host:port/db-name"
PORT=8080
CORS_ORIGIN="http://localhost:5173"

# Voice transcription (Groq Whisper) — server-managed, not user-configurable
GROQ_VOICE_API_KEY="gsk_..."

# WebSocket URL returned to paired devices (defaults to wss://tunnel.droidclaw.ai)
# WS_URL="wss://tunnel.droidclaw.ai"
